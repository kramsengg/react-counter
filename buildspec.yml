version: 0.2

phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build
  post_build:
    commands: 
      - aws s3 cp --recursive ./buil s3://awspipelinedemo/
      - aws cloudfront create-invalidation --distribution-id test --path /\*
